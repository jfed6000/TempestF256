# lwasm build products (the coco-shelf wrapper writes these next to the source)
*.list
*.map

# Built OS-9 modules (make -C src rebuilds them; list each one here as it is added)
/src/tempest

# Editor and tool leavings
*~
*.bak
.#*
\#*#
__pycache__/
*.pyc
.DS_Store

# tempest_orig/ is Atari's source as released: keep every file in it, whatever its name
!/tempest_orig/**

# Stage 0 captures and renders (tools/avgcap.lua, tools/avgview.py): regenerable
/captures/

# D6 pilot builds (tools/d6pilot.py assembles pilot/*.a here)
/pilot/build/

# The translator's output (tools/m65to09.py writes it; regenerable)
/xlat/
